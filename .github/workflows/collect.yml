# AI ニュースを定期収集して docs/ を更新し、そのままサーバーへ配信する
name: collect

on:
  schedule:
    # 1 時間に 1 回。これ以上短くすると Google ニュース検索のスクレイピングがブロックされるリスクが上がるため、更新頻度の上限の目安としている
    - cron: "0 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: collect
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install build tools
        run: npm ci
      - name: Build site (minify JS/CSS/HTML into docs/)
        run: npm run build
      - name: Collect
        run: node scripts/collect.mjs
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # state.json は次回の「新着」判定に使うので一緒にコミットする
          git add docs data/state.json
          git diff --cached --quiet && echo "no changes" && exit 0
          git commit -m "chore: collect $(date -u +%Y-%m-%dT%H:%MZ)"
          git push
      # GITHUB_TOKEN による push は deploy ワークフローを起動しないので、ここでもアップロードする
      - name: Upload docs/ to Lolipop (FTP)
        if: ${{ vars.DEPLOY_TARGET == 'lolipop' }}
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.LOLIPOP_FTP_SERVER }}
          username: ${{ secrets.LOLIPOP_FTP_USER }}
          password: ${{ secrets.LOLIPOP_FTP_PASSWORD }}
          protocol: ftps
          local-dir: ./docs/
          server-dir: ${{ vars.LOLIPOP_SERVER_DIR }}
          dangerous-clean-slate: false
